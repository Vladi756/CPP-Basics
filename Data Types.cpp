#include <iostream>

using namespace std;

int main()
{
    char grade = 'A';                   // A character
    string phrase = "Hello";            // A string is a chracter array - so like a word
    int number = 5;                     // Whole numbers
    float average = 5.4;                // Floating point number
    double specificNumber = 5.45;       // Doubles can store more decimals than floats
    boolean isMale = true;              // True/False

    return 0;
}
